{
  "type": "module",
  "scripts": {
    "build": "node scripts/build-dist.mjs",
    "check:wrangler": "npm run build && wrangler pages dev dist --compatibility-date=2026-04-28",
    "deploy:cloudflare": "npm run build && wrangler pages deploy dist --project-name=aiwebminar --commit-dirty=true",
    "test:security": "node tests/security-smoke.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.84.1"
  }
}
